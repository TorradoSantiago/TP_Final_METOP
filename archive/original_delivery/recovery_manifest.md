# Recovery Manifest

- No notebook or empirical source file was ever committed inside the git history of `TP_Final_METOP`; the history only contains `README.md` and later blueprint files.
- A likely original final-delivery notebook was found in the sibling repository `ejercicios.regresion` under the filename `rosario_voting_analysis.ipynb`.
- The notebook itself contains the heading `TRABAJO FINAL` and a Colab link that points to `TPFINAL_VOTOS_ROSARIO.ipynb`, which is why it was copied here under that recovered name.
- The earliest README from `TP_Final_METOP` was also restored so the original minimal state of the repo is preserved.
